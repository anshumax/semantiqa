{
  "name": "@semantiqa/app-main",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "prebuild": "pnpm --filter @semantiqa/app-config run build",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "pnpm run build && electron ."
  },
  "dependencies": {
    "@semantiqa/app-config": "workspace:^0.1.0",
    "@semantiqa/contracts": "workspace:^0.1.0",
    "@semantiqa/graph-service": "workspace:^0.1.0",
    "electron": "^32.3.3",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@semantiqa/storage-sqlite": "workspace:^0.1.0",
    "better-sqlite3": "^9.5.0"
  }
}
