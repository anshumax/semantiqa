{
  "name": "@semantiqa/app-preload",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/preload.js",
  "types": "dist/preload.d.ts",
  "scripts": {
    "prebuild": "pnpm --filter @semantiqa/app-config run build",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@semantiqa/app-config": "workspace:^0.1.0",
    "electron": "^32.3.3"
  }
}
